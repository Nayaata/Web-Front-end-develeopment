<div class="container-fluid">
    <div class="row">
        <h1>Images</h1>
        <div class="col-xs-12">
            {{#images}}
            <div class="col-xs-4">
                <a><img class="img-thubnail" src="{{imageSource}}" alt=""></a>
                <p>{{description}}</p>
            </div>
            {{/images}}
        </div>
    </div>
</div>