<div class="container">
    <div class="row">
        <h1 class="gallery">ACTIVE COURSES</h1>
        {{#courses}}
        <div class="col-sm-4 courses">
            <div class="panel panel-primary ">
                <div class="panel-heading bluebg" style="text-align: center; background-color: #466289;">{{title}}</div>
                <div class="panel-body"><img src="{{coursesSource}}" class="img-thubnail" style="position: relative; left: 15px" alt=""></div>
                <div class="panel-footer">{{description}}</div>
            </div>
        </div>
        {{/courses}}
    </div>
</div>